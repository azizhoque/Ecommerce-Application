services:
  postgres:
    container_name: ms_pg_sql
    image: postgres
    environment: 
      POSTGRESQL_USERNAME: postgres
      POSTGRESQL_PASSWORD: root123
volumes:
  postgres:
  pgadmin:
  mongo: